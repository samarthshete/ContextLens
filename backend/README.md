# ContextLens Backend

RAG evaluation and debugging platform — FastAPI backend.

## Setup

```bash
pip install -e .
uvicorn app.main:app --reload
```

## Health

`GET /health` — returns `{"status": "ok"}`

# ContextLens Backend

FastAPI backend for the ContextLens RAG evaluation and debugging platform.